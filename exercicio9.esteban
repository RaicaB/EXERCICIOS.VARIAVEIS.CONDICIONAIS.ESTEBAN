using System;
class Program
{
    static void Main()
    {
        int contador = 1;
        int soma = 0;

        while (contador <= 100)
        {
            soma = soma + contador;
            contador++;
        }

        Console.WriteLine("A soma dos números de 1 a 100 é: " + soma);
    }
}
